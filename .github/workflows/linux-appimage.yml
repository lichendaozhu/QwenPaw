name: Linux AppImage

on:
  push:
    branches:
      - codex/linux-appimage-v2.1.0
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-linux-appimage:
    # Keep the build on the oldest supported Ubuntu baseline.  The AppImage
    # linuxdeploy/GTK path is currently unreliable on ubuntu-latest (24.04).
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux desktop build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            desktop-file-utils \
            file \
            fuse \
            libfuse2 \
            libappindicator3-dev \
            libglib2.0-dev \
            libgtk-3-dev \
            libtbb12 \
            libtbbmalloc2 \
            librsvg2-dev \
            libwebkit2gtk-4.1-dev \
            patchelf \
            zsync

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install packaging helper dependencies
        run: python -m pip install packaging

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: console/package-lock.json

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Linux AppImage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_OPTIONS: "--max-old-space-size=8192"
          APPIMAGE_EXTRACT_AND_RUN: "1"
        run: bash scripts/pack-tauri/build_linux_appimage.sh

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: QwenPaw-Tauri-v2.1.0-Linux-x86_64-AppImage
          path: dist/QwenPaw-Tauri-*-Linux-x86_64.AppImage
